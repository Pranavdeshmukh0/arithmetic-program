#include<stdio.h>

int main()
{
        int a,b,mul;
        printf("\n\tmultiplication of two numbers:");
        scanf("%d%d",&a,&b);
        mul=a*b;
        printf("\n\t%d",mul);
        return 0;
}
